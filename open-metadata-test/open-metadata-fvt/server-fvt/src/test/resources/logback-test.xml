<?xml version="1.0" encoding="UTF-8"?>

<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright Contributors to the ODPi Egeria project. -->

<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="org.odpi.openmetadata.serverfvt" level="INFO" />

    <!-- Rejected calls are the expected outcome of much of this suite - most of the validation coverage is
         a call that is supposed to fail - and each one logs an error
         server-side.  Keep that expected noise out of the build output; a test that wants to see the
         message reads it from the exception it caught, not from the console. -->
    <logger name="org.odpi.openmetadata.userauthn" level="OFF" />

    <root level="warn">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>
