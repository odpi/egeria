<?xml version="1.0" encoding="UTF-8"?>

<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright Contributors to the ODPi Egeria project. -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <artifactId>open-connectors</artifactId>
        <groupId>org.odpi.egeria</groupId>
        <version>3.13-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <scm>
        <connection>scm:git:git://github.com/odpi/egeria.git</connection>
        <developerConnection>scm:git:ssh://github.com/odpi/egeria.git</developerConnection>
        <url>http://github.com/odpi/egeria/tree/main</url>
    </scm>

    <name>Repository Services Connectors</name>
    <description>
        The repository-services-connectors contains connector implementations for
        each type of connector supported by the Open Metadata Repository Services (OMRS).
        These connectors enable the OMRS to be adapted to many different platforms.
    </description>

    <artifactId>repository-services-connectors</artifactId>

    <packaging>pom</packaging>
    <modules>
        <module>audit-log-connectors</module>
        <module>open-metadata-collection-store-connectors</module>
        <module>cohort-registry-store-connectors</module>
        <module>open-metadata-archive-connectors</module>
    </modules>

    <dependencies>

        <dependency>
            <groupId>org.odpi.egeria</groupId>
            <artifactId>repository-services-apis</artifactId>
        </dependency>

    </dependencies>

</project>
