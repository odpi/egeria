/* SPDX-License-Identifier: Apache-2.0 */
/* Copyright Contributors to the ODPi Egeria project. */
package org.odpi.openmetadata.accessservices.subjectarea.server.mappers.relationships;

import org.odpi.openmetadata.accessservices.subjectarea.properties.relationships.Synonym;
import org.odpi.openmetadata.repositoryservices.connectors.stores.metadatacollectionstore.properties.instances.Relationship;
import org.testng.annotations.Test;

import static org.testng.Assert.assertEquals;
import static org.testng.Assert.assertNotNull;

/**
 * Test Synonym mapper
 */
public class TestSynonymMapper
{
    public static final String DESC = " ssss";
    @Test
    public void testSynonymMapper1()
    {
//        Synonym synonym = new Synonym();
//
//        String beforeDescription = DESC;
//        synonym.setDescription(beforeDescription);
//        // TODO mock the helper ?
//        SynonymMapper mapper = new SynonymMapper(null);
//        Relationship relationship = mapper.mapLineToRelationship(synonym);
//        Synonym afterSynonym = (Synonym) mapper.mapRelationshipToLine(relationship);
//        assertNotNull(afterSynonym);
//        assertEquals(DESC,afterSynonym.getDescription());
    }
}
